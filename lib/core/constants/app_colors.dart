import 'package:flutter/material.dart';

class AppColors {

  // Uygulamanın ana mor rengi (Stumedia Moru)
  static const Color primary = Color(0xFF6C63FF);

  // Vurgu ve ikincil işlemler için turkuaz
  static const Color secondary = Color(0xFF03DAC6);


  // Genel sayfa arka planı (Kırık beyaz/Gri)
  static const Color background = Color(0xFFF9F9F9);

  // Kartların ve kutucukların arka planı (Tam beyaz)
  static const Color surface = Colors.white;


  // Ana başlıklar için koyu renk (Tam siyah yerine koyu gri daha modern durur)
  static const Color textPrimary = Color(0xFF2D2D2D);

  // Açıklamalar ve alt başlıklar için gri
  static const Color textSecondary = Color(0xFF757575);


  // Hata mesajları için kırmızı
  static const Color error = Color(0xFFB00020);

  // Başarılı işlemler için yeşil
  static const Color success = Color(0xFF4CAF50);


  // Kenarlıklar ve çizgiler için açık gri
  static const Color border = Color(0xFFE0E0E0);
}